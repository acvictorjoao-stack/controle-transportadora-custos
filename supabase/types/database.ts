/**
 * Tipos gerados do banco de dados Supabase.
 *
 * Será preenchido nas próximas sprints com `supabase gen types typescript`.
 * Por enquanto, exporta um placeholder para manter a estrutura da pasta.
 */

export type Database = Record<string, never>;
